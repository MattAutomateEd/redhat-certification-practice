---
ansible-navigator:
  execution-environment:
    container-engine: podman
    enabled: true
    image: ee-supported-rhel8:latest
    pull:
      policy: missing
  format: yaml
  mode: stdout
  playbook-artifact:
    enable: false
